# Disposable permission probe

This file exists only to test whether the connected GitHub plugin requires confirmation before marking a pull request ready or merging it into an isolated scratch base branch. It must never reach `main`.
